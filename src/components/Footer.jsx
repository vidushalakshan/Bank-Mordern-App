import React from 'react';
import { logo } from '../assets';

const Footer = () => {
  return (
    <section>
        <div>
            <div>
                <img src={logo} alt="hoobank" />
            </div>
        </div>
    </section>
  )
}

export default Footer;